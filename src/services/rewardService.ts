export const rewardService = {
    getRewards: async () => [],
    claimReward: async (payload: any) => ({ success: true }),
    awardXP: async (payload: any) => ({ newTotal: 0, badgeUnlocked: false }),
};
